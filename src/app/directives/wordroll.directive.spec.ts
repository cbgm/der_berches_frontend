import { WordrollDirective } from './wordroll.directive';

describe('WordrollDirective', () => {
  it('should create an instance', () => {
    const directive = new WordrollDirective();
    expect(directive).toBeTruthy();
  });
});
